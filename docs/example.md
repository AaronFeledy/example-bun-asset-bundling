# Example Documentation

This markdown file is embedded using a build plugin (METHOD 4).

It demonstrates that you can bundle files from specific directories using custom build plugins, even if those files aren't directly imported in your code.

## Features

- Bundled as a file asset
- Accessible via `Bun.embeddedFiles` at runtime
- Can be read and used like any other embedded file
